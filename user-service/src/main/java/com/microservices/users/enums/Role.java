package com.microservices.users.enums;

/**
 * Enum representing the different roles a user can have in the system.
 */
public enum Role {
    ADMIN,
    USER,
    RESTAURANT_OWNER,
    DELIVERY_PERSON
}